# Feature Index

> Central tracking for all features. Updated by skills automatically.

## Status Legend
- **Roadmap** - `/init` done, feature identified in feature map, no spec file yet
- **Planned** - `/write-spec` done, full spec written, architecture not yet designed
- **Architected** - `/architecture` done, tech design approved, ready to build
- **In Progress** - `/frontend` or `/backend` active or completed, not yet in QA
- **In Review** - `/qa` active, testing in progress
- **Approved** - `/qa` passed, no critical/high bugs, ready to deploy
- **Deployed** - `/deploy` done, live in production

## Features

| ID | Feature | Status | Priority | Depends on | Spec | Created |
|----|---------|--------|----------|-----------|------|---------|
| PROJ-1 | Supabase Infrastructure Setup | Approved | P0 | None | [PROJ-1](PROJ-1-supabase-infrastructure-setup.md) | 2026-06-17 |
| PROJ-2 | User Authentication & Profile | Deployed | P0 | PROJ-1 | [PROJ-2](PROJ-2-user-authentication-and-profile.md) | 2026-06-17 |
| PROJ-3 | Photo Upload & Space Scan | Deployed | P0 | PROJ-1, PROJ-2 | [PROJ-3](PROJ-3-photo-upload-and-space-scan.md) | 2026-06-17 |
| PROJ-4 | Environmental Data Enrichment | Deployed | P0 | PROJ-3 | [PROJ-4](PROJ-4-environmental-data-enrichment.md) | 2026-06-17 |
| PROJ-5 | Plant Database & Admin Interface | Deployed | P0 | PROJ-1, PROJ-2 | [PROJ-5](PROJ-5-plant-database-and-admin-interface.md) | 2026-06-17 |
| PROJ-6 | Rule-Based Plan Generation | Deployed | P0 | PROJ-3, PROJ-4, PROJ-5 | [PROJ-6](PROJ-6-rule-based-plan-generation.md) | 2026-06-17 |
| PROJ-7 | Plan Review & Acceptance | Deployed | P0 | PROJ-6 | [PROJ-7](PROJ-7-plan-review-and-acceptance.md) | 2026-06-17 |
| PROJ-8 | Shopping List & Deep Links | Deployed | P0 | PROJ-7 | [PROJ-8](PROJ-8-shopping-list-and-deep-links.md) | 2026-06-17 |
| PROJ-9 | Progress Photo Log | Roadmap | P1 | PROJ-7 | — | 2026-06-17 |
| PROJ-10 | In-App Notifications | Planned | P1 | PROJ-5, PROJ-6 | [PROJ-10](PROJ-10-in-app-notifications.md) | 2026-06-22 |
| PROJ-11 | Plant Catalogue ETL (FloraWeb/BfN + AI trait mapping) | Deployed | P1 | PROJ-1, PROJ-5 | [PROJ-11](PROJ-11-plant-catalogue-etl-floraweb.md) | 2026-07-06 |
| PROJ-12 | AI Plan Curation & Rationale | Deployed | P1 | PROJ-6, PROJ-7 (soft: PROJ-11) | [PROJ-12](PROJ-12-ai-plan-curation-and-rationale.md) | 2026-07-07 |
| PROJ-13 | Survival Confidence Band | Deployed | P1 | PROJ-6, PROJ-7 (soft: PROJ-4, PROJ-11) | [PROJ-13](PROJ-13-survival-confidence-band.md) | 2026-07-10 |
| PROJ-14 | Ecological Trait Enrichment (ETL extension) | In Progress | P1 | PROJ-5, PROJ-11 | [PROJ-14](PROJ-14-ecological-trait-enrichment.md) | 2026-07-10 |
| PROJ-15 | Biodiversity Indicator | Planned | P1 | PROJ-7, PROJ-14 | [PROJ-15](PROJ-15-biodiversity-indicator.md) | 2026-07-10 |

<!-- Add features above this line -->

## Build Order
PROJ-1 → PROJ-2 → PROJ-3 → PROJ-4 → PROJ-5 → PROJ-6 → PROJ-7 → PROJ-8 → PROJ-9 → PROJ-10 → PROJ-11

Note: the P0 journey (PROJ-1–8) is deployed and now iterates; P1 features (PROJ-9, PROJ-10, PROJ-11) build on top and are independent of each other. PROJ-11 (catalogue expansion) only needs PROJ-5's table + PROJ-1's seed pattern; it enlarges the palette PROJ-6 plans from, with two PROJ-6 engine changes (consume `moisture`, weight `native`) left as separate follow-ons.

Notes:
- **PROJ-14 backend built (2026-07-10, /backend):** the pipeline half — no API route (PROJ-14 has no runtime surface; that's PROJ-15). Migration `20260710110000_proj14_plants_ecological_traits.sql` adds 6 additive nullable columns (`insect_value`/`bird_value` check none/low/medium/high, `bloom_start_month`/`bloom_end_month` check 1–12, `pollinator_friendly` boolean, separate `eco_ai_origin_fields` text[]) + a guarded both-or-neither bloom constraint; no RLS/backfill, PROJ-6 unaffected. `scripts/lib/catalogue.mjs`: `importPlantSchema` + `confidenceSchema` extended (insect/bird/pollinator required, bloom pair nullable both-or-neither with `end<start` = valid wrap), **one** `review_required` gate now fed by survival AND ecological low-confidence, `buildStagedRow` seeds the separate `eco_ai_origin_fields`, **`SYNCABLE_FIELDS`** gains the 5 eco columns + `eco_ai_origin_fields` (the whole backfill; `ai_origin_fields` deliberately NOT syncable so survival provenance is never clobbered; order-insensitive array compare), `planSync` now skips `review_required` rows, new `ecologicalCoverageReport`. `ai-traits.mjs` infers the 5 traits + 4 eco confidences (enum-locked bands, nullable bloom). `import-plants-sync.mjs` prints coverage over a fresh post-update read; `staging.mjs` curator header covers eco + naturadb.de verification. Suite 417 → 440 green, lint + build clean. **⚠️ Deploy gate: apply the migration via dashboard SQL Editor** (admin saves error against live until then). Remaining is curator/deploy: run the live pipeline (wildlife values first), record the naturadb.de→band mapping + coverage. Next: `/qa`. See PROJ-14 spec → Implementation Notes — Backend.
- **PROJ-14 frontend built (2026-07-10, /frontend):** the feature's only UI surface — the admin plant-form **"Ecological traits" side door** for rows the pipeline won't touch. `src/lib/plants.ts` gains the shared eco contract (`WILDLIFE_VALUE_OPTIONS`, `MONTH_OPTIONS`, `ECOLOGICAL_TRAIT_FIELDS` with the bloom pair as one `bloom_period` provenance entry; `Plant` + `plantSchema` extended with the 5 nullable columns + separate `eco_ai_origin_fields`; both-or-neither bloom-pair rule with year-wrap valid; `bloomPeriodSummary` ready for PROJ-15). `plant-form.tsx` gains the fieldset: explicit "Not assessed" ⟷ NULL on every select (honest default, never a guess), tri-state pollinator select (`null` ≠ `false`), "AI-inferred — not yet verified" chips driven by `eco_ai_origin_fields`. Suite 401 → 417 green, lint + build clean. **⚠️ The form now sends the eco columns on every save — admin plant saves error against the live DB until the PROJ-14 migration lands. Next (immediately): `/backend`** (migration, ETL inference + review gate, `SYNCABLE_FIELDS` backfill, coverage report). See PROJ-14 spec → Implementation Notes — Frontend.
- **PROJ-13 deployed (2026-07-10, /deploy):** live on https://sproutly-green.de via main-push auto-deploy (`dpl_8YPHFTrAaoxrq4WLemMzii8BfAEu`, commit `b89dd94`, READY in ~47s). The push also shipped the PROJ-4 enrichment fix (QA BUG-1/BUG-2) and four earlier never-pushed spec/PROJ-11-pipeline commits. Migration was already applied at /backend; no env vars or n8n changes. Post-deploy checks: routes + auth redirects correct, zero Vercel runtime errors; rollback candidate `dpl_9VcUhJxnL3DGKnEHrkVEUfyiZe28`. Tagged `v1.13.0-PROJ-13`. Residual: authenticated 390px eyeball of the plan screen (headline card, band chips, picker pills). See PROJ-13 spec → Deployment.
- **PROJ-13/14/15 specced (2026-07-10):** the two "score" ideas from the PRD's non-goals, reframed as honest, deterministic, banded indicators — no fabricated percentages, AI kept in the data-enrichment role only, never generating a user-facing number. **PROJ-13 Survival Confidence Band** — a graded version of PROJ-6's existing hard filters (soil/moisture match, data-gap and location-basis downgrades, native/maintenance as offset-only boosts), shown on the plan view + add-plant picker; feeds engine *ranking* only (hard filters unchanged, guardrail intact). **PROJ-14 Ecological Trait Enrichment** — extends the PROJ-11 ETL to infer + human-verify per-species ecological traits (insect/bird value, bloom months, pollinator flag), gated on verification because of the documented ~40% native-flag AI error rate; backfills the live catalogue via the existing sync step. **PROJ-15 Biodiversity Indicator** — plan-view band + concrete data-backed claim ("supports pollinators & birds, blooms ~7 months"), computed from PROJ-14's *verified-only* data with honest partial-coverage disclosure. Build order: PROJ-13 is independent (ship any time); PROJ-15 depends on PROJ-14 shipping + backfilling first. PRD non-goals updated to point at the reframed features.
- **PROJ-13 backend built (2026-07-10, /backend):** the deterministic confidence band implemented headless — new PURE module `src/lib/plan-confidence.ts` (single source of truth: per-plant band + machine-readable reason codes, headline aggregation via majority-band-plus-exceptions with ties going to the LOWER band, rainfall bucketed at read time behind `RAINFALL_LOW_MAX_MM = 600` / `RAINFALL_HIGH_MIN_MM = 1000`, DWD-cited); engine ranking now band-FIRST within each layer with the old native → soil → maintenance → compact → name order as tiebreak (hard filters untouched — 252-site guardrail passes unchanged); migration `20260710100000_proj13_plan_confidence_snapshot.sql` adds nullable `plans.snapshot_rainfall_mm` + `plans.snapshot_location_basis` (**applied to production 2026-07-10** — no remaining deploy gate); `persistGeneratedPlan` + `applyCuration` both write the enlarged snapshot; `confidenceSiteFromPlan` (plans.ts) feeds the UI; bands computed, never persisted; `isPlanStale` deliberately ignores the new fields (else every pre-PROJ-13 plan turns stale on ship). Suite 337 → 383 green, lint + build clean. Next: `/frontend` (headline card, per-plant badge, picker chips, reason-code copy — no %/guarantee wording). See PROJ-13 spec → Implementation Notes.
- **PROJ-13 frontend built (2026-07-10, /frontend):** the display layer, all thin renderers of the pure module so surfaces can't disagree — new `src/components/plans/plan-confidence-view.tsx` (ALL reason-code → plain-language copy lives here, the one place enforcing no-%/no-score/no-"guarantee"; band = icon + text chip, never colour alone): `PlanConfidenceHeadline` (majority band + explicit exception counts, site-level gap attribution "a gap in our site data, not in the plants", or the checked-evidence line when site data is complete), `ConfidenceBadge` (per-plant chip + tap-to-expand reasons: mismatch copy names the conflict AND the fix, direction-specific moisture advice, offset gaps stay visible), `ConfidenceChip` (band pill on every add-picker candidate row). PlanEditor derives everything at render via `confidenceSiteFromPlan` + `plantConfidence` + `summarizePlanConfidence`, so headline/badges/picker recompute together on every add/remove. Two consolidations: the per-line "May not suit your soil" badge retired (the band's soil-mismatch reason is the authoritative display per spec) and the zone-unconfirmed banner now renders only on empty plans (the headline carries that gap otherwise). Open questions resolved: care-notes link = soil-mismatch reason appends "see the care tips below" when notes exist; visual treatment = tinted chips (forest / sage / terracotta) with per-band icons (ShieldCheck / CircleCheck / Eye). Suite 383 → 395 green, lint + production build clean. Next: `/qa`. See PROJ-13 spec → Implementation Notes — Frontend.
- **PROJ-13 QA passed (2026-07-10, /qa) — Approved.** 17/17 ACs, all edge cases handled, security clean (new snapshot columns verified live under RLS + check constraints, incl. a cross-user "band poisoning" attempt). New tests: `plan-editor.test.tsx` (4 — headline/badges/picker wiring + live recompute + legacy-zone-banner consolidation) and `tests/PROJ-13-confidence-rls-isolation.spec.ts` (7, seeded ephemeral accounts). Final suites: unit 399/399, e2e 92/92, zero regressions. **Two enrichment bugs found (PROJ-4 root, not PROJ-13 regressions): BUG-1 Medium** — a partial DWD grid failure stores fabricated `rainfall_mm = 0` with `climate_status = 'success'`, which PROJ-13 would snapshot as real and force false "Worth checking" moisture conflicts on wet-moisture plants; **BUG-2 High, pre-existing in production** — same `?? 0` root fabricates `minTemp 0` → hardiness zone '10' marked *confirmed*, silently disabling the PROJ-6 winter hard filter. Both fixable in one small `/backend` change (`fetchDwdClimate` per-field nulls + per-field status in `enrichment/run.ts`); strongly recommended before/with the PROJ-13 deploy. See PROJ-13 spec → QA Test Results.
- **PROJ-4 post-deploy fix (2026-07-10, /backend):** BUG-1 + BUG-2 from PROJ-13's QA fixed — `DwdClimate` fields are now per-field nullable (an unsampled DWD grid stays null, never a fabricated 0), the hardiness zone is derived only from a genuinely sampled min temp (unsampled → `zone_status: 'unavailable'`, PROJ-6's honest zone-unconfirmed path, instead of a fake confirmed zone '10' that disabled the winter hard filter), unsampled climate columns stay NULL (PROJ-13's `siteRainfall` then skips the moisture factor instead of judging against fake 0 mm), and enrichment reports `'partial'` unless all three climate fields sampled so retry stays reachable. +2 regression tests (`enrichment/run.test.ts`), suite 401/401, lint + build clean. No schema change; rows with a previously fabricated value self-heal on that scan's next enrichment run. PROJ-13 has no open bugs — clear to `/deploy`. See PROJ-4 spec → "Post-Deploy Fix — partial DWD grid failure".
- **PROJ-11 deployed (2026-07-09):** live curator session — cross-checked the full staging catalogue against naturadb.de (native status + survival-critical fields + common_name), confirming the QA-flagged `native`-flag risk was real and systemic (GBIF-derived native status wrong, always false-when-should-be-true, on ~40% of a fresh candidate batch — not a one-off). Expanded `CANDIDATE_ALLOWLIST` by 39 species (1 dropped post-review: *Erigeron karvinskianus*, invasive per naturadb.de; 1 failed AI inference: *Quercus robur*). Added a third pipeline step, **`npm run import:plants:sync`** (`scripts/import-plants-sync.mjs`, `planSync`/`SYNCABLE_FIELDS` in `scripts/lib/catalogue.mjs`) — `import:plants:commit`'s `ON CONFLICT DO NOTHING` can never push a correction into an already-live row by design, so corrections to existing rows had no path to production without it; sync only touches approved rows whose live `source === 'open_data_etl'`, never a hand-seeded or admin-edited row. Ran the full pipeline live: catalogue grew 141 → 160 plants; verified rendering correctly in `/admin/plants` via a real browser screenshot. Known residual: two hand-seeded rows (*Achillea millefolium*, *Betula pendula*) keep their pre-ETL common_name since the sync guard correctly won't touch non-ETL-owned rows — a manual admin edit, not a pipeline fix. See PROJ-11 spec → "Live Curator Session".
- **PROJ-12 deployed (2026-07-09):** merged to `main` via PR #14 and live on Vercel. All deploy gates satisfied — `N8N_CURATE_WEBHOOK_URL`/`N8N_CURATE_SECRET` set in Vercel, migration applied, n8n "Plan Curation" workflow Active (Expression-mode prompt, `claude-haiku-4-5` to stay under the 15s route timeout), and a live production smoke-test passed (curated plan + rationale end-to-end; silent rule-engine fallback confirmed safe). Loose end: add the `N8N_CURATE_*` placeholder lines to `.env.local.example` (env-file writes blocked by local permissions — docs-only, production is configured). See PROJ-12 spec → Deployment.
- **PROJ-12 backend built (2026-07-08, /backend):** the PRD **Plan** AI swap-in point implemented as augmentation — new `/api/curate-plan` (auth-guarded, re-derives survivors server-side, n8n "Plan Curation" workflow via `N8N_CURATE_WEBHOOK_URL`/`N8N_CURATE_SECRET`, 15s timeout, silent `{curated:false}` on any failure), pure validation core `src/lib/plan-curation.ts` (menu/bounds/length checks + `applyCuration` → engine-shaped plan, quantities via `computeQuantities`, re-checked by `findConstraintViolations`), curation wired inside `persistGeneratedPlan` (both PlanBuilder and Regenerate get it; fallback = exactly today's plan), rationale persisted as `plans.rationale_intro` + `plan_plants.rationale` (migration `20260708100000_proj12_plan_rationale.sql` — **apply via dashboard SQL Editor**), edits carry rationale through `replacePlanLines`, plan view shows the "Why this plan" intro card + per-plant why lines (plain text). Suite 337/337 green. **Deploy gates:** apply migration, set the two env vars (+ add to `.env.local.example` — blocked by local permissions this session), import/activate `docs/n8n/plan-curation.workflow.json`, live smoke-test. See PROJ-12 spec → Implementation Notes.
- PROJ-4 (Environmental Enrichment) is split from PROJ-3 (Scan): separate concern, 3 external APIs, and a blocking open question on the BGR endpoint. The scan stores manual-form data on its own; enrichment augments it.
- PROJ-5 (Plant DB & Admin) must precede PROJ-6 (Plan Generation) — the rule engine can't run without seeded, rule-tagged plants.
- **Carried from PROJ-1 QA → PROJ-2:** runtime E2E verification of magic-link sign-in + auto-provisioned profile (AC-3), own-row RLS read/update + cross-user denial (AC-5/AC-6), and storage isolation upload/deny (AC-7/AC-8). PROJ-1 verified these structurally; PROJ-2's E2E suite must prove them against two real accounts.
- **Carried from PROJ-5 QA → PROJ-6 (now owned in the PROJ-6 spec):** two Low bugs about `plants.image_url`, deferred to where the image is first rendered (the plan view in PROJ-6). **BUG-1** — `image_url` has no DB-level format constraint (client `plantSchema` validates format only); add a `check (image_url ~ '^https?://')` or an app-layer guard. **BUG-2** — `plantSchema`'s `z.string().url()` accepts non-http(s) schemes (`javascript:`/`data:`); restrict to http(s) and render `image_url` only via a safe `<img src>` with an http(s) allowlist. Both are now in PROJ-6's scope (Acceptance Criteria → "Image safety" + Technical Requirements). See PROJ-5 "QA Test Results" → Bugs found.
- **PROJ-10 (In-App Notifications) split from PROJ-7 (2026-06-22):** the "your plan was updated" reassignment notice (records + banner + My Spaces indicator, structured for a v2 inbox/bell/push) is its own P1 feature. PROJ-7 keeps plan editing, the Order seam, staleness, and the duplicate-line merge. PROJ-10 only needs PROJ-5 + PROJ-6 (its trigger) and surfaces on the plan view + My Spaces; it can be built any time after PROJ-6 but is sequenced after the P0 journey.
- **PROJ-3 post-deploy fix (2026-06-24):** authenticated "Save space" failed with `permission denied for function gen_scan_short_code` — the short-code insert trigger called a helper whose EXECUTE is revoked from `authenticated`, as a plain (invoker-rights) function. Fixed by making `set_scan_short_code()` SECURITY DEFINER (migration `20260624110000_proj3_fix_short_code_trigger_execute.sql`, applied to production). Also improved the save-error toast to surface the real Supabase error. See PROJ-3 spec → "Post-Deploy Fix — short-code trigger permission".
- **PROJ-7 post-deploy enhancement (2026-06-24):** the plan view now shows a short per-plant care-notes blurb (surfaces the existing, already-seeded `plants.care_notes`; frontend-only, no DB change). See PROJ-7 spec → "Post-Deploy Enhancement — Per-plant care notes on the plan".
- **Post-login landing simplified (2026-06-24):** removed the static welcome screen at `/`; it now redirects — no scans → `/scans/new` (scan form), has scans → `/scans` (My Spaces). Profile/admin nav moved to the `/scans` header. See PROJ-3 spec → "Post-Deploy Enhancement — Skip the welcome screen on login".
- **PROJ-3 post-deploy enhancement (2026-06-24):** the scan photo is now **optional** — users can create a scan from the conditions questions alone (lowers friction for the "Guilty Non-Starter" persona). The other fields stay required. Migration `20260624100000_proj3_photo_optional.sql` (drops `NOT NULL` on `scans.photo_path`) — **apply via dashboard SQL Editor before/with the next deploy.** See PROJ-3 spec → "Post-Deploy Enhancement — Optional photo".
- **PROJ-3 post-deploy enhancement (2026-06-23):** replaced the 36-char scan UUID in URLs (`/scans/<uuid>/...`) with an 8-char `scans.short_code` (`/scans/Kp3xR9aQ/...`), affecting the PROJ-7 plan and PROJ-8 shopping-list URLs too. UUID stays the PK for all internal references. Migration `20260623100000_proj3_scan_short_code.sql` applied to production (via dashboard SQL Editor — not in CLI migration history, which this project doesn't use). Chose "Replace": old UUID bookmarks now 404, no redirect fallback. Verified live. See PROJ-3 spec → "Post-Deploy Enhancement — Short URL code".

- **PROJ-3 post-deploy enhancement (2026-07-02):** added a **"Use my location" postcode fallback** in `scan-form.tsx` for photos without GPS EXIF (screenshots, EXIF-stripped shares) or when the photo is skipped — reads `navigator.geolocation` and reverse-geocodes it via the existing `/api/geocode` route (no new route, no env var, no DB change). Refactored the geocode call into a shared `geocodeToPostcode()` helper and made the auto-fill hint source-aware (photo vs current location). Co-located `scan-form.test.tsx` +2 tests (now 4); full suite 194/194 green. Frontend-only, live-ready. See PROJ-3 spec → "Post-Deploy Enhancement — Use my location postcode fallback".

- **PROJ-3 post-deploy enhancement (2026-07-02):** wired the **AI scan-vision prefill** (the PRD Scan swap-in point) into `scan-form.tsx` — picking a photo uploads it and calls `POST /api/classify-vision` (n8n + Claude vision), prefilling sun/surface/space_type/area on a confident read with a "Reading your space…" → "we filled these in" flow; degrades silently to the manual form. Backend route + n8n workflow already existed; this is the UI glue. Co-located `scan-form.test.tsx` (2 tests), full suite 192/192 green. **Now live (deployed 2026-07-06):** n8n instance imported + `N8N_CLASSIFY_WEBHOOK_URL`/`N8N_CLASSIFY_SECRET` set in Vercel (see `docs/n8n-scan-vision-workflow.md`); the "Reading your space…" vision prefill runs in production. This is the PRD **Scan** AI swap-in point, now fully realized. See PROJ-3 spec → "Post-Deploy Enhancement — AI scan-vision prefill, UI wiring".

- **PROJ-3 post-deploy enhancement (2026-07-02):** re-shaped `/scans/new` into the prototype's **3-step scan wizard** (`upload → reading → review`, `docs/design-references/screen_02–04.png`) — big "Scan your space" dropzone, a "Reading your space…" interstitial around the AI vision prefill, then a "Here's what we see" review with design-system form-row cards and a "Looks right — show me my plan" CTA. Added a `variant="hero"` to `photo-picker.tsx`; new-scan header now shows the `1/4` step counter + 4-dot journey progress. **Current data model only** — no new orientation/maintenance fields (deferred). Edit mode skips the wizard. Frontend-only, 194/194 green, live-ready. See PROJ-3 spec → "Post-Deploy Enhancement — 3-step scan wizard matching the prototype".

- **PROJ-7 + PROJ-3 post-deploy enhancement (2026-07-03):** three tweaks from local-preview feedback ("postcode still not auto-filled; the primary button should lead to the plan screen and show the conditions there, smaller"), frontend-only, no DB change. (1) **Land straight on the plan** — the scan-review CTA / "Generate plan" button now navigate to `/scans/{code}/plan`; arriving with no plan yet **auto-builds** it in place via the new `PlanBuilder` (waits ≤12 s for async enrichment over Realtime, then runs the shared `persistGeneratedPlan` from new `src/lib/plans-client.ts`, reused by `GeneratePlanButton`) instead of bouncing back to the scan. (2) **Compact conditions on the plan screen** — new `PlanConditions`/`ConditionChips` chip strip (`plan-conditions.tsx`) replaces the full-size "Based on your conditions" card in `PlanEditor` (sourced from the plan *snapshot*, still honest when stale) and heads the auto-build screen (current conditions); co-located `plan-conditions.test.tsx` (+5). (3) **Prominent "Use my location"** — the postcode fallback button in `scan-form.tsx` upgraded from a tiny link to an outlined button. Suite 241→246 green, build + lint clean. See PROJ-7 → "Post-deploy tweaks" and PROJ-3 → "Use my location" fallback follow-up.

- **SOLID refactor, round 2 (2026-07-03):** three follow-ups from the same review, all behavior-preserving, no DB change. (4) **One soil vocabulary** — new dependency-free `src/lib/soil.ts` is the single source (plants/scans/bgr now import it), and every Zod schema derives its enums from the option arrays via `optionValues()` in `src/lib/utils.ts` (scans, plants, profile, classify-vision route) — adding an option can no longer leave a schema silently rejecting it. (5) **Plan-engine quantity maths deduplicated** — `densityFor`/`layerAreaSqm`/`quantityFor`/`capQuantities` are now shared by `generatePlan` and the PROJ-7 `computeQuantities` rebalance (previously two hand-kept copies that could drift); verified equivalent by the full engine + 252-site guardrail suites. (6) **Client persistence pattern completed** — new `plants-client.ts` (savePlant, deletePlantWithReassign) and `profile-client.ts` (updateProfile, uploadAvatar, removeAvatar; the uploader is now the SOLE owner of `users.avatar_path`, ending the form/uploader dual-write); `plans-client.ts` gains `replacePlanLines` used by the plan editor — **insert-new-then-prune-old** replaces the non-atomic delete-then-insert whose failure mode silently emptied a plan (duplicates during a failed prune are already collapsed by `mergeDuplicateLines`); `scans-client.ts` gains `deleteScan` which deletes the ROW first so a failed delete no longer destroys the photo. Co-located tests for all new modules; suite 222→241 green, build + lint clean.

- **SOLID refactor, PROJ-3/PROJ-4 internals (2026-07-03):** behavior-preserving decomposition from a SOLID review. (1) `scan-form.tsx` (689→434 lines) — all I/O moved to `src/lib/scans-client.ts` (upload/classify/save/enrich-trigger, mirroring `plans-client.ts`), vision prefill + "use my location" extracted to `src/hooks/use-vision-prefill.ts` / `use-locate-postcode.ts`, wizard step screens to `scan-wizard-steps.tsx`, FieldRow to `scan-field-row.tsx`. (2) `/api/enrich` (330→84 lines) — orchestration/climate/persistence moved to `src/lib/enrichment/{run,climate,store}.ts`; the duplicated hand-rolled Nominatim clients unified in `src/lib/nominatim.ts` (shared by `/api/geocode` + enrichment). (3) New `src/lib/api.ts` `requireUser()`/`parseJson()` guards adopted by all four API routes (each route must self-check auth since middleware exempts `/api/*`). Co-located tests added for every new module; suite 194→222 green, build + lint clean. No DB/runtime behavior change.

- **PROJ-6 post-deploy enhancement (2026-06-29):** added a **survival-constraint guardrail** — `findConstraintViolations()` re-derives from the plan's own snapshot that every recommended plant clears the hard sun/zone/fit filters (independent of `matchingSurvivors`), with a co-located test that both detects tampered lines and runs the engine over the real seed catalogue across a 252-site matrix asserting zero violations. Test-only regression net for "the planner recommends a plant that can't survive this site"; no DB/runtime change. See PROJ-6 spec → "Post-Deploy Enhancement — Survival-constraint guardrail".

- **PROJ-3 post-deploy enhancement (2026-07-06):** made the postcode **auto-fill in the common cases** (the recurring "postcode still not auto-filled" feedback — photo-GPS auto-fill only fires when the photo actually carries GPS EXIF, which most mobile uploads/screenshots/messenger-shared photos don't). Two additions, frontend-only, **no DB change / no migration**: (1) **Remember the last postcode** — `/scans/new` now reads the user's most recent scan's postcode (RLS-scoped) and passes it to `ScanForm` as `defaultPostcode`, pre-filling with a "From your last space — edit if needed" hint (kept "untouched" so a geotagged photo can still correct it). (2) **Auto-geolocate on the review step** — a new scan reaching review with no postcode yet quietly attempts `navigator.geolocation` once (no tap); added a `silent` option to `use-locate-postcode.ts` so this un-requested attempt degrades with no error toast on denial, while the explicit "Use my location" button still toasts. Precedence: remembered → photo GPS → auto-geolocate → button/manual. Co-located tests updated + new `use-locate-postcode.test.ts`; full suite 251/251 green. See PROJ-3 spec → "Post-Deploy Enhancement — Postcode auto-fill (remember last + auto-geolocate)".

## Next Available ID: PROJ-16
